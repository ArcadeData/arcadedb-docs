name: arcadedb
title: ArcadeDB
version: ~
nav:
  - modules/ROOT/nav.adoc
  - modules/ROOT/nav-query.adoc
  - modules/ROOT/nav-api.adoc
asciidoc:
  attributes:
    revnumber: '26.5.1'
    project-name: 'ArcadeDB'
    site-url: 'https://docs.arcadedb.com'
    repo-url: 'https://github.com/ArcadeData/arcadedb'
    # doctype: book — allows multiple level-1 sections per page, which
    # comes up in legacy files that have intro content before the first
    # heading (e.g. reference/graph-algorithms/algorithms.adoc).
    doctype: 'book'
    # Placeholders intentionally left as literals in REST-API URLs / code samples.
    # Defining them as themselves silences the "missing attribute" warnings that
    # Asciidoctor emits when a substitution target is undefined.
    database: '{database}'
    language: '{language}'
    db: '{db}'
    query: '{query}'
    mongo: '{mongo}'
    command: '{command}'
    incombiningdiacriticalmarks: '{incombiningdiacriticalmarks}'
